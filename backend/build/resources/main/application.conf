ktor {
    deployment {
        port = 8080
    }
    application {
        modules = [ com.kappa.backend.ApplicationKt.module ]
    }
}

database {
    url = "jdbc:postgresql://45.59.114.105:5432/kappa?sslmode=require"
    driver = "org.postgresql.Driver"
    user = "appuser"
    password = "123456789"
}

jwt {
    issuer = "kappa"
    audience = "kappa-audience"
    realm = "kappa"
    secret = "change_this_secret"
    accessTokenTtlSeconds = 3600
    refreshTokenTtlSeconds = 604800
}

livekit {
    url = "ws://10.0.2.2:7880"
    apiKey = "devkey"
    apiSecret = "devsecret"
    tokenTtlSeconds = 3600
}
