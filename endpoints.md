# Endpoints (placeholders)

These are temporary defaults and can be replaced later.

## WebSocket
- `GAME_WS_URL`: `wss://example.com/games`

## REST API base URLs
- `ADMIN_API_BASE`: `https://api.appkappa.com/api/admin/`
- `RESELLER_API_BASE`: `https://api.appkappa.com/api/reseller/`
- `AGENCY_API_BASE`: `https://api.appkappa.com/api/agency/`

## Notes
- Update `app/build.gradle.kts` when real endpoints are ready.
- All values are also available through `BuildConfig`.
- See `admin_endpoints.md`, `reseller_endpoints.md`, and `agency_endpoints.md` for placeholder route lists.
