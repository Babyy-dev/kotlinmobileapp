# Image Asset Mapping (Potalive-style)

## Header / Background
- `potalive_top_header.png` (632x170 px) ? Top art header on Rooms, Home (Inbox), Profile, Admin, Agency, Reseller.
- `potalive_header.png` (697x161 px) ? Ornate header panel behind tab labels (Rooms/Home).
- `potalive_header_preview.png` (697x161 px) ? Preview variant (optional).
- `potalive_bottom_nav.png` (1127x330 px) ? Bottom art band under navigation.

## Navigation Icons
- `ic_nav_rooms.xml` (vector) ? Bottom nav: Rooms tab icon.
- `ic_nav_inbox.xml` (vector) ? Bottom nav: Message/Inbox tab icon.
- `ic_nav_profile.xml` (vector) ? Bottom nav: Profile tab icon.

## Room Detail Section Icons
- `ic_room_audio.png` (170x170 px) ? Room section: Audio.
- `ic_room_seats.png` (158x158 px) ? Room section: Seats.
- `ic_room_chat.png` (176x176 px) ? Room section: Chat.
- `ic_room_gifts.png` (158x158 px) ? Room section: Gifts.
- `ic_room_tools.png` (192x192 px) ? Room section: Tools.
- `ic_room_ornament.png` (140x140 px) ? Room section ornament.

## Buttons / Pills / Badges
- `btn_potalive_teal.png` (697x161 px) ? Primary CTA button background.
- `bg_pill_gold.xml` (shape) ? VIP / highlighted labels.
- `bg_pill_teal.xml` (shape) ? Secondary status labels.
- `bg_inbox_badge.xml` (shape) ? Inbox unread badge.

## Cards / Rows / Containers
- `bg_room_thumbnail.xml` (shape) ? Room card image frames.
- `bg_room_row.xml` (shape) ? Room list row background.
- `bg_card_glass.xml` (shape) ? Glass panel lists.
- `bg_admin_panel.xml` / `bg_admin_panel_soft.xml` (shape) ? Dashboard cards.
- `bg_profile_header.xml` (shape) ? Profile header panel.

## Search / Input
- `bg_inbox_search.xml` (shape) ? Inbox search bar background.
- `bg_dropdown.xml` (shape) ? Country dropdown background.

## Logo
- `logo.png` (862x1024 px) ? App logo (scaled with fitCenter in login/signup/onboarding/splash).

## Needed to Match Potalive (Not in project yet)
- Country flag icons (full ISO) ? chips, room cards, country list. (Recommend 48x48 px)
- Room card thumbnails (real images) ? Rooms list. (Recommend 640x360 px)
- Gift animation sprites (burst/coin/box) ? Room gift overlay. (Sizes vary; provide sprite sheets or frame PNGs)
- VIP/level badges ? Room cards + profile header. (Recommend 96x96 px)
- Inbox icons (assistant/announcement/bell) ? Home/Inbox rows. (Recommend 96x96 px)

